# 数码耳机案例验收单

## 演示目标

- 素材配置：产品特写图、使用场景图、产品演示视频、优惠文案。
- 验证重点：素材推荐解释、CTA 包装卡、`high_click` 脚本与 `strong_hook` 渲染差异。

## 已有自动化证据

- `ai-services/tests/test_assets_api.py` 覆盖素材匹配与推荐输出。
- `ai-services/tests/test_render_api.py` 覆盖 PNG 包装卡和 `strong_hook` 可播放渲染。

## 2026-05-27 接口验收记录

- 环境：隔离数据库及媒体目录，Celery eager 模式，真实模型配置值不写入文档。
- 能力状态：LLM 显示已提供配置；Vision 使用占位描述；ASR 与 AIGC 未启用。
- 执行结果：创建项目成功；首次上传 3 秒合成样例进行分析时，模型服务返回 `401 Unauthorized`，链路在结构抽取阶段中止。
- 结论：在更新本地 Doubao 授权并重新完成分析、脚本和渲染前，本案例不得标记为演示通过。

## 待完成记录项

| 项目 | 记录 |
| --- | --- |
| 输入素材与授权来源 | 待凭证修复后完成真实验收 |
| 补全策略与来源图例截图 | 待凭证修复后完成真实验收 |
| 分析 / 脚本 / 渲染耗时 | 分析中止于模型授权检查，未记录成功耗时 |
| `original` 与 `strong_hook` 输出路径 | 待生成，目标目录为 `data/demo-cases/headphones/` |
